# header
some tests

## Usage

1. Run the service:

  ```sh
  docker compose up -d --build
  ```

2. Open <http://localhost:8080/src/Main.elm>.

3. Stop the service:

  ```sh
  docker compose down --remove-orphans --volumes
  ```
